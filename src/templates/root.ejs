<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crisis Pregnancy Centers</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="bg-gray-100 text-gray-900">

<!-- Include Navbar Partial -->
<%- include('partials/navbar') %>

<!-- Main Content -->
<div class="grid grid-cols-12 py-12 pt-32 md:py-36 relative pb-20 md:pb-36 md:pt-56 block bg-[#001524] text-gray-50">
    <div class="col-centered tracking-wide uppercase md:px-0 px-4 col-span-12 text-6xl md:text-8xl font-extrabold text-center">
        <div class="text-center col-centered">
            CPC Directory
        </div>
        <div class="px-2 md:px-2 md:px-2 md:px-8 max-w-6xl">
            <div class="normal-case tracking-normal font-bold mt-4 text-lg md:text-xl">
                We aim to stop dishonest Canadian charities and organizations from restricting a woman’s access to the reproductive care that is best for her.
                Check this directory to find out if the organization you are considering provides the services you need.
            </div>
            <div class="mt-4">
                <a href="#search_brands">
                    <div class="text-2xl hover:scale-110 tracking-wide cursor-pointer shadow-md duration-200 border-[#D6CC99] hover:bg-[#D6CC99] text-[#D6CC99] hover:text-[#001524] p-4 px-6 border-4 inline-block rounded-full">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor" class="relative z-10 -top-0.5 inline-block w-8 h-8">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-5.197-5.197m0 0A7.5 7.5 0 105.196 5.196a7.5 7.5 0 0010.607 10.607z"></path>
                        </svg> Search
                    </div>
                </a>
            </div>
        </div>
    </div>
</div>
<main class="max-w-4xl mx-auto p-6 bg-white shadow-md rounded-lg mt-6">
    <h1 class="text-3xl font-bold mb-4">Welcome to the Crisis Pregnancy Centers Directory</h1>
    <p class="text-gray-700 mb-4">Browse our list of crisis pregnancy centers and learn more about each organization.</p>
    <ul class="list-disc pl-6">
        <% cpcs.forEach(cpc => { %>
            <li>
                <a href="/cpc/<%= cpc.slug %>.html" class="text-blue-500 hover:text-blue-700 font-semibold underline">
                    <%= cpc.name %>
                </a>
            </li>
        <% }) %>
    </ul>
</main>

<!-- JavaScript for toggling the mobile menu -->
<script>
    const mobileMenuButton = document.getElementById('mobile-menu-button');
    const mobileMenu = document.getElementById('mobile-menu');

    mobileMenuButton.addEventListener('click', () => {
        console.log("mobile menu cilck");
        const isExpanded = mobileMenuButton.getAttribute('aria-expanded') === 'true';
        mobileMenuButton.setAttribute('aria-expanded', !isExpanded);
        mobileMenu.classList.toggle('hidden');
    });
</script>

</body>
</html>
